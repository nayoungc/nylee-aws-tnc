export interface CourseLab {
  catalogId: string;
  labId: string;
  moduleId: string;
  labNumber: string;
  title?: string;
  description?: string;
  duration?: string;
  difficulty?: string;
  instructions?: string;
}
